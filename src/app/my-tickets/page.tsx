import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "StagePass | My Tickets",
  description: "My Tickets",
};

export default function MyTicketsPage() {
  return (
    <section>
      <h1>My Tickets</h1>
    </section>
  );
}
